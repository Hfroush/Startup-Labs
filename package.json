{
  "name": "startup-labs-website",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "description": "Startup Labs Global — static marketing site served from deploy/. No build step. See ARCHITECTURE.md.",
  "scripts": {
    "preview": "python3 -m http.server 4599 --directory deploy"
  }
}
