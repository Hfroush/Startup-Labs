# Server port (defaults to 5000)
PORT=5000

# PostgreSQL connection string (only needed if switching from in-memory to DB storage)
# DATABASE_URL=postgresql://user:password@host:5432/dbname

# Node environment
NODE_ENV=development
